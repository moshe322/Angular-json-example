import { ProductspipePipe } from './productspipe.pipe';

describe('ProductspipePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductspipePipe();
    expect(pipe).toBeTruthy();
  });
});
